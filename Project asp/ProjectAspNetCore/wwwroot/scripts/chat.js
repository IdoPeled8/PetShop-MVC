    //var connection = new signalR.HubConnectionBuilder()
    //.withUrl("/chathub")
    //.build();

    //connection.on("NewMessage", (userName, message) => {
    //    $("#chatLog").html($("#chatLog").html() + " Message from " + userName + ": " + message + "<br/>");
    //});

    //connection.start().catch(err => console.error(err.toString()));

    //$("#all").click(function () {
    //    var userName = $("#username").val();
    //var message = $("#message").val();
    //    connection.invoke("MessageAll", userName, message);
    //});
