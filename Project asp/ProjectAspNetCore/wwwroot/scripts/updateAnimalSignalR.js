//var connection = new signalR.HubConnectionBuilder().withUrl("/chatHub").build();

//// Rest of your existing code...

//// Handle the "UpdateAnimal" event from the hub
//connection.on("UpdateAnimal", function (animal) {

//    // Update the UI with the new animal data
//    // For example, append the animal details to a table, or update an existing item in the list
//    // You can use jQuery or any other JavaScript framework for this task.
//});

//// Start the connection
//connection.start().catch(function (err) {
//    console.error(err.toString());
//});
